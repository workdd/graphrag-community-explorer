# Copy to .env.local (ignored by Git) to open a local folder under public/data/ on start.
# Files: entities.parquet, relationships.parquet, communities.parquet, community_reports.parquet,
# optionally <label>_communities.parquet listed in manifest.json.
VITE_DEFAULT_DATA=./data/my-index
